import Foundation

enum SharedModelContainer {
    /// Must match the App Group configured in Signing & Capabilities for both
    /// the main app target and the widget extension target.
    static let appGroupIdentifier = "group.com.personal.logbook"
}
